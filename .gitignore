# Foundry — files that must never leave your machine

# User data & session state (contains API keys in plain text)
data.json
data.json.bak
templates.json

# Uploads land inside project folders
mc-uploads/

# Node & OS
node_modules/
*.log
.env
.env.local

# IDE
.idea/
.vscode/
*.swp

# Mac / Windows junk
.DS_Store
Thumbs.db
desktop.ini

# Credentials backups (from account switcher)
credentials-backups/
*.credentials.json
